api:
  host: "0.0.0.0"
  port: 8000
  reload: true
  workers: 1

redis:
  host: "localhost"
  port: 6379
  db: 0

mongodb:
  uri: "mongodb://localhost:27017"
  database: "polyguard"
  results_collection: "scan_results"

security:
  secret_key: "change-me-in-production"
  algorithm: "HS256"
  access_token_expire_minutes: 60
